/**
 * Schema-v3 release policy. Serialized artifacts carry this value for
 * diagnostics, but validation must never treat artifact metadata as policy.
 */
export const PRICE_MODEL_RELEASE_GATE_MINIMUM_TEST_SIZE = 30;
